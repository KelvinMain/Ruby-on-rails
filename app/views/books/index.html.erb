<p id="notice"><%= notice %></p>

<% if request.original_url.split('=').last.include? '/' %>
<h1>All Books</h1>
<% else %>
<h1>All <%= request.original_url.split('=').last.sub! '+',' ' %> Books</h1>
<% end %>

<% @books.each do |book| %>
  <h2><%= link_to book.title, book %></h2>
  <h4> <%= truncate(book.author, length: 200) %> </h4>
  <p>
  <%= truncate(book.description, length: 200) %>
  </p>
<% end %>
